# GST-9700 Realtime Diagnostic UI

The repository now includes a realtime diagnostic panel for the physical GST-9700 serial stream. It exposes connection state, RX packet count, last RX time, parsed weight, raw frame, stale/live status, stability flags, reconnect count and parser errors.
